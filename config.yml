# Database settings.
database:
  driverClass: org.h2.Driver
  user: sa
  password: sa
  url: jdbc:h2:./target/transactions

logging:
  level: INFO
  loggers:
    io.dropwizard.archetypes: DEBUG
